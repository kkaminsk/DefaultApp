## Tasks

- [x] Add `PingDnsButtonText` and `PingDnsButtonBackground` properties to `MainViewModel.cs`
- [x] Add `PingDnsCommand` and `PingDns` method following the gateway ping pattern
- [x] Add `NotifyCanExecuteChangedFor` attribute to `DnsServer` property
- [x] Update `MainPage.xaml` to add Ping button next to DNS Server copy button
- [x] Add localization strings for the ping button in `Resources.resw`
- [x] Build and verify the feature works correctly
