<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DefaultApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DefaultApp"
    xmlns:views="using:DefaultApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Default App">

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Grid
            x:Name="AppTitleBar"
            Grid.Row="0"
            Height="48"
            VerticalAlignment="Top"
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="188" />
            </Grid.ColumnDefinitions>

            <!-- App Icon and Title -->
            <StackPanel
                Grid.Column="0"
                Orientation="Horizontal"
                Spacing="12"
                Margin="16,0,0,0"
                VerticalAlignment="Center">
                <Image
                    Source="Assets/StoreLogo.png"
                    Width="16"
                    Height="16" />
                <TextBlock
                    x:Uid="AppTitleText"
                    Text="Default App"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                    VerticalAlignment="Center" />
            </StackPanel>

            <!-- Drag Region (spacer) -->
            <Border Grid.Column="1" x:Name="DragRegion" Background="Transparent" />

            <!-- Theme Selector and Info Button -->
            <StackPanel
                Grid.Column="2"
                Orientation="Horizontal"
                Spacing="4"
                VerticalAlignment="Center"
                Margin="0,0,8,0">
                <ComboBox
                    x:Name="ThemeSelector"
                    x:Uid="ThemeSelector"
                    MinWidth="160"
                    TabIndex="0"
                    AutomationProperties.Name="Theme selector">
                    <ComboBoxItem x:Uid="ThemeSystemDefault" Content="System Default" />
                    <ComboBoxItem x:Uid="ThemeInverted" Content="Inverted" />
                </ComboBox>
                <Button
                    x:Name="AboutButton"
                    Click="AboutButton_Click"
                    ToolTipService.ToolTip="About"
                    Background="Transparent"
                    BorderThickness="0"
                    Padding="8"
                    VerticalAlignment="Center"
                    AutomationProperties.Name="About">
                    <FontIcon
                        FontFamily="{StaticResource SymbolThemeFontFamily}"
                        Glyph="&#xE946;"
                        FontSize="16" />
                </Button>
            </StackPanel>

            <!-- Space for window controls -->
            <Border Grid.Column="3" />
        </Grid>

        <!-- Main Content -->
        <views:MainPage x:Name="MainPageContent" Grid.Row="1" />
    </Grid>
</Window>
